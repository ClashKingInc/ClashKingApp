org.gradle.jvmargs=-Xmx4G -Dfile.encoding=UTF-8
android.useAndroidX=true
# Temporary upstream compatibility: Flutter's Gradle plugin still needs the
# old DSL, and app_links/home_widget/workmanager still apply the Kotlin plugin.
android.builtInKotlin=false
android.newDsl=false
