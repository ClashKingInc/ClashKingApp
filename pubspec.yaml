name: clashkingapp
description: "The ClashKing companion app for Clash of Clans players and clans."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the build number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.3.5+25
environment:
  sdk: '>=3.10.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.5+1
  app_links: ^7.2.1
  shared_preferences: ^2.5.4
  http: ^1.6.0
  crypto: ^3.0.7
  clipboard: ^3.0.8
  lucide_icons_flutter: ^3.1.14+1
  fl_chart: ^1.1.1
  tuple: ^2.0.2
  custom_sliding_segmented_control: ^1.8.5
  dropdown_button2: ^3.1.0
  home_widget: ^0.9.3
  workmanager_android: ^0.9.0+2
  workmanager_platform_interface: ^0.9.1+1
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  sentry_flutter: ^9.25.0
  retry: ^3.1.2
  sliver_tools: ^0.2.12
  flutter_secure_storage: ^10.0.0
  collection: ^1.19.1
  flutter_gif: ^0.0.4
  flutter_svg: ^2.3.0
  package_info_plus: ^10.2.0
  device_info_plus: ^13.2.0
  mailto: ^2.0.0
  timeago: ^3.7.1
  dio: ^5.8.0
  universal_html: ^2.3.0
  cupertino_icons: ^1.0.8
  liquid_glass_widgets: ^0.21.3
  # 0.2.13 calls UITab.selectedImage, which is absent in the Xcode 26.5 SDK.
  native_liquid_glass: 0.2.12
  intl: ^0.20.2
  url_launcher: ^6.3.2
  open_filex: ^4.6.0
  webview_flutter: ^4.14.1
  flutter_native_splash: ^2.4.7
  path_provider: ^2.1.6
  share_plus: ^13.2.0
  flutter_file_saver: ^0.10.0
  flutter_soloud: ^4.0.12
  audio_session: ^0.2.2
  uuid: ^4.5.1
  clashking_design_system:
    git:
      url: https://github.com/ClashKingInc/DevKit.git
      ref: main
      path: design/packages/flutter
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  flutter_local_notifications: ^22.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_launcher_icons: ^0.14.4
  mockito: ^5.6.1
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  generate: true

  fonts:
    - family: CustomIcons
      fonts:
        - asset: fonts/CustomIcons.ttf

  assets:
    - assets/icons/
  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding assets from package dependencies,
  # see https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/icon-play-store.png"
  image_path_ios: "ios/Runner/Assets.xcassets/AppIcon.appiconset/Icon-App-1024x1024@1x.png"


flutter_native_splash:
  color: "#ffffff"
  image: assets/icons/app_icon_light_mode.png
  color_dark: "#000000"
  image_dark: assets/icons/app_icon_dark_mode.png
  android: true
  ios: true
  image_ios: assets/icons/splashIOSlight.png
  color_ios: "#ffffff"
  image_dark_ios: assets/icons/splashIOSdark.png
  color_dark_ios: "#000000"
  android_gravity: center
  ios_content_mode: scaleAspectFit
  web: true

  android_12:
    image: assets/icons/splash12light.png
    icon_background_color: "#ffffff"
    image_dark: assets/icons/splash12dark.png
    icon_background_color_dark: "#000000"
